# Watchable - Extension to Events in Node.js

Usage
<code><pre>
var Watchable = require('watchable')

var MyClass = <Inherit from Watchable>

var myobject =  new MyClass
myobject.watch(this)
</pre></code>

